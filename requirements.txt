# ============================================================
# AI Voice Analysis System - Python dependencies
# Install everything with:  pip install -r requirements.txt
# ============================================================

# --- Core ML / DL ---
#tensorflow==2.15.0          # deep learning (use tensorflow-gpu build for CUDA)
scikit-learn>=1.3.0         # encoders, scalers, metrics, train/test split
numpy==1.26.4               # numerical arrays
joblib==1.4.2               # saving/loading sklearn objects

# --- Audio processing ---
librosa==0.10.2.post1       # feature extraction (MFCC, mel, pitch, etc.)
soundfile==0.12.1           # reading/writing audio files (librosa backend)
audioread==3.0.1            # mp3 decoding support

# --- GUI ---
streamlit==1.39.0           # web interface (provides st.audio_input for mic)

# --- Plotting ---
matplotlib==3.8.4           # waveform, spectrogram, confusion matrix, curves

# --- Data / dashboard ---
pandas==2.2.2               # history dashboard tables and charts

# --- Exports ---
reportlab==4.2.0            # PDF report generation

# --- Optional (advanced features) ---
# transformers==4.44.0      # uncomment for pretrained wav2vec2 / accent models
# torch==2.3.1              # uncomment if you prefer PyTorch backends
# noisereduce==3.0.2        # stronger noise reduction than the built-in method
streamlit>=1.28.0
librosa>=0.10.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
plotly>=5.17.0
scipy>=1.11.0
soundfile>=0.12.0
joblib>=1.3.0
reportlab>=4.0.0